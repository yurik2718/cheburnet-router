#!/bin/sh /etc/rc.common
# Apply VPN mode based on slider state on boot (runs after podkop)

START=99
USE_PROCD=1

boot() { start; }

start_service() {
    # Give podkop a moment to come up fully, then sync to slider
    ( sleep 5; /usr/bin/vpn-mode detect; /usr/bin/vpn-led ) &
}
