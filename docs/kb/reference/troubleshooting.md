---
title: Troubleshooting — куда смотреть
tags: [reference]
aliases: [troubleshooting, диагностика, отладка]
updated: 2026-08-22
---

# Troubleshooting — куда смотреть

> [!tip] TL;DR
> 90% жалоб «нет интернета» решаются тремя командами: статус [[amneziawg|туннеля]], статус
> режима, последние логи. Начинай с них.

## Первые три команды

```bash
ucode -R /usr/share/cheburnet/engine/invariants/gather.uc \
  | ucode -R /usr/share/cheburnet/engine/invariants/check.uc   # чек-лист «что на месте» + чем чинить
ubus call cheburnet status           # то, что видит панель: режим, туннель, DNS
logread | tail -200                  # что случилось недавно (сторож пишет только про поломки)
```

## По симптому

### «Нет интернета вообще»
1. `awg show awg0` — есть свежий handshake? Нет → проблема с [[amneziawg|туннелем]]/конфигом.
2. **Handshake свежий, панель зелёная, а интернета нет** — спроси у ядра, кто владеет дефолтом:
   ```bash
   ip route get 1.1.1.1        # ожидается dev awg0 (или singtun0), а НЕ dev <WAN>
   ip route show | grep '/1 '  # half-routes 0.0.0.0/1 + 128.0.0.0/1 через туннель — на месте?
   ```
   Уехало на WAN → не-direct трафик уходит без метки и его режет [[kill-switch]]. Вернуть
   (эта же команда переводит установку со старой схемы маршрута на half-routes, см. [[reliability]]):
   ```bash
   ucode -R /usr/share/cheburnet/engine/steps/vpn/apply.uc --arm
   ```
3. [[kill-switch]] работает как задумано: туннель упал → непрямой трафик дропается (не утечка).
   Это **не баг**, а защита. Чини туннель.

### «Сайт из direct-списка идёт через VPN, а должен напрямую»
Проверь, попал ли его адрес в [[dnsmasq-nftset|direct]]:
```bash
nft list set inet fw4 direct         # есть ли там нужный IP
nslookup example.com                  # резолвится через наш dnsmasq?
```
Если домена нет в твоём direct-списке (или в импортированном community-списке) — его адрес
туда не попадёт. См. [[dnsmasq-nftset]].

**Адрес в наборе ЕСТЬ, а трафик всё равно идёт через VPN.** Значит сломана вторая половина моста:
метка ставится, а направлять помеченный трафик нечем. Спроси ядро напрямую — оно ответит честнее,
чем конфиг:
```bash
ip route get <IP-из-набора> mark 0x1   # ожидается dev <WAN>, а НЕ dev awg0/singtun0
ip rule show | grep fwmark             # правило направления должно быть
ip route show table 100                # в таблице direct должен быть default через WAN
```
Пусто? Верни правила одной командой (она же зовётся при загрузке роутера):
```bash
ucode -R /usr/share/cheburnet/engine/install/reapply.uc
```
Так выглядел реальный баг: правила policy-routing живут только в ядре и не переживали
перезагрузку, поэтому после каждого ребута direct-домены уходили в туннель при полностью зелёной
панели. Восстановление держит hotplug-хук `/etc/hotplug.d/iface/99-cheburnet` — если файла нет,
переприменение настройки из панели (смена режима) вернёт и его.

### «Ничего не резолвится» / «интернет пропал совсем»
Сначала выясни, жив ли резолв и какой цепочкой он идёт:
```bash
nslookup openwrt.org 127.0.0.1            # работает ли наш dnsmasq вообще
pgrep -a https-dns-proxy                   # ДВА экземпляра: :5053 (основной) и :5054 (резервный)
ip rule show | grep uidrange                # правило резервного пути на месте?
```
Ожидается два процесса под РАЗНЫМИ пользователями (`nobody` и `network`) и правило
`uidrange <uid network> lookup 100`. Нет правила — резервный путь не включён, и смерть туннеля
унесёт с собой весь резолв; вернуть его вместе с остальным data-plane:
```bash
ucode -R /usr/share/cheburnet/engine/install/reapply.uc
```
Нет второго процесса — переприменить DoH-шаг **с вашим провайдером** (без stdin шаг возьмёт дефолт
каталога и молча сменит фильтрацию): `printf '{"provider":"adguard"}' | ucode -R /usr/share/cheburnet/engine/steps/doh/apply.uc`.
Сторож делает это сам раз в 5 минут, если туннель цел.
Почему так устроено — [[encrypted-dns]].

### «Туннель не поднять, а интернет нужен прямо сейчас»
В панели, в блоке состояния при мёртвом туннеле — кнопка **«Выключить защиту (аварийно)»**.
Трафик пойдёт напрямую, мимо VPN; настройки сохранятся, вернуть — кнопкой «Вернуть защиту».
Из терминала то же самое:
```bash
ucode -R /usr/share/cheburnet/engine/install/pause.uc            # выключить защиту
ucode -R /usr/share/cheburnet/engine/install/pause.uc --resume   # вернуть
```
Пока режим включён, защиту никто не вернёт молча — ни сторож, ни перезагрузка. Подробно:
[[kill-switch]].

### «Реклама лезет»
Блокировку даёт **выбранный DNS-провайдер**, а не локальный список — проверь, что выбран
фильтрующий резолвер и что запросы реально идут через него:
```bash
uci get https-dns-proxy.cheburnet_doh.resolver_url   # какой резолвер выбран
nslookup ads.example.com                              # фильтрующий вернёт пустой/0.0.0.0
```
Не фильтрует → смени провайдера в панели (`ads`/`family`). Учти: реклама с общего CDN-домена
DNS-блокировкой не режется. См. [[adblock]], [[0005-dns-filtering-not-local-adblock]].

### «Клиент не слушается split-routing»
Возможно, у устройства **свой DoH** (Chrome, смарт-ТВ) — оно не резолвит через нас.
Это [[dns-and-routing#Зависимость от того, что клиент использует НАШ DNS|известный размен]]:
такой клиент уходит в туннель (fail-safe), прямого пути для доменов из списка у него не будет.

### Мастер вместо шага «Проверка роутера» показывает экран смены LAN-адреса

Это не баг — конфликт подсетей LAN/WAN, детект **до** запуска обычного мастера. Возникает,
когда cheburnet-router висит за другим роутером (каскад) в одной и той же подсети
`192.168.1.0/24` — тогда policy routing и kill-switch не могут отличить «локальную» сеть от
«внешней» через LAN-интерфейс.

Экран предлагает свободный адрес для смены LAN — можно применить его или пропустить (конфликт
останется, но основной preflight заблокирует установку явным сообщением, а не тихо). Если
застрял на этом экране без явной причины — сверь `192.168.1.x` со своим апстрим-роутером (тот,
что раздаёт cheburnet-router интернет по WAN).

## Проверка плоскости данных

```bash
ip rule show                         # видны ли наши policy-routing правила
ip route show table 100              # таблица «напрямую через WAN»
nft list ruleset | less              # все правила фаервола и kill-switch
```

Поскольку весь [[data-plane]] — на нативных примитивах, всё **видно и проверяемо** командами.
Это прямое следствие «без чёрных ящиков» ([[0001-why-not-singbox]]).

## Логи установки

```bash
cat /tmp/cheburnet/install.log       # лог последней установки
cat /tmp/cheburnet/done              # маркер завершения
```

## Диагностический пакет (для тех, кто просит помощи)

Кнопка **«Собрать диагностику»** в панели (или из терминала — то же самое) собирает логи,
состояние сети, версии и конфигурацию в один текст:

```bash
ucode -R /usr/share/cheburnet/engine/install/diagnostics.uc          # человеку
ucode -R /usr/share/cheburnet/engine/install/diagnostics.uc --json   # для UI
```

**Секреты вырезаются до вывода** — приватные/пресхаред-ключи WireGuard/AmneziaWG, пароли
Hysteria2 и обфускации, UUID и `pbk`/`sid` у Reality, пароли sing-box, пароль Wi-Fi. Список
вырезанного печатается в шапке пакета, панель показывает текст **до** скачивания — не «доверьтесь
нам», а видно своими глазами. Direct-домены не печатаются, только размер набора.

**Адрес/порт сервера, SNI, тип обфускации и ошибки остаются намеренно** — подключиться по ним
нельзя, а без них причину не найти.

Код: `engine/lib/redact.uc` (чистка, юниты `engine/lib/tests/test_redact.uc`),
`engine/install/diagnostics.uc` (сбор). Роутер сам никуда пакет не отправляет — только
показывает для ручной отправки.

## Сброс настройки: что снимается, а что остаётся

Кнопка «Сбросить настройку cheburnet» (или `ucode -R engine/install/reset.uc`) — это **не удаление
программы**, и путать эти вещи вредно: человек нажимает её, ожидая чистый роутер, а получает
роутер без VPN, но с установленным пакетом.

**Снимается:** правила и наборы nft, ip-правила и таблица policy routing, NAT-зона; секции туннеля
в `network` (обе группы — AmneziaWG и sing-box, независимо от активного протокола); привязки
dnsmasq (наши ipset-секции, DoH-апстримы, `noresolv`, `strictorder`); `https-dns-proxy` целиком;
`sing-box`, если Full-тир ставился (сервис, uci-секция, `config.json`); hotplug-хук
`/etc/hotplug.d/iface/99-cheburnet`; cron-запись сторожа; содержимое `/etc/cheburnet`.

**Остаётся:** пакеты (`apk del` — осознанно забота владельца), движок в `/usr/share/cheburnet`,
обработчик rpcd и веб-панель, Wi-Fi и пароль root. Панель после сброса открывается и показывает
мастер.

Два момента, оплаченных инцидентами:

- **`network restart`, а не `reload`** — reload не возвращает WAN-дефолт после удаления
  `awg0`-интерфейса (netifd держит остаточный маршрут через мёртвый туннель), роутер остаётся
  без интернета. Тот же урок, что в `rollback_all`.
- **Повторная настройка не требует SSH** — install-токен одноразовый и раньше пропадал вместе с
  каталогом при сбросе, оставляя человека без доступа к мастеру («токен не найден — запустите
  bootstrap по SSH»). Теперь его по осознанному действию владельца выдаёт ubus-метод
  `install_token` (auth=admin, access=write), панель зовёт его и ведёт в мастер ссылкой с
  токеном; повторный вызов отдаёт тот же токен, а не новый.

## Дальше

- [[data-plane]] — что и как устроено
- [[kill-switch]] — почему «дроп» это норма
- [[README|Карта знаний]]
